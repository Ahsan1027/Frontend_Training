import styled from 'styled-components';
const OrdersOverviewWrapper = styled.div`

.custom-size{
    width: 295px;
height: 400px;
flex-shrink: 0;
border: 1px solid var(--components-bradcrumb-fill, #E9ECEF);
background: var(--global-09-white, #FFF);
}
`;

export default OrdersOverviewWrapper;